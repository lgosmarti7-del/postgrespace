# 📤 Entrega del Set 05 — Vistas, funciones y procedimientos

## Qué entregar por ejercicio

| Ejercicio | Archivos | Contenido clave |
|---|---|---|
| **Paso 1** | `paso1.sql` + `paso1.png` | Las dos vistas (`mascotas_con_tutor` y `historial_consultas`); captura de `SELECT * FROM historial_consultas` |
| **Paso 2** | `paso2.sql` + `paso2.png` | Las dos funciones (`costo_total_tutor` y `mascotas_sin_consulta`); captura de los 4 `SELECT costo_total_tutor(...)` |
| **Paso 3** | `paso3.sql` + `paso3.png` | Los dos procedimientos con llamadas de prueba y `SELECT` de verificación; captura del `NOTICE` en pgAdmin |

## Dónde van tus archivos

```
entregas/
└── apellido_nombre/
    └── 05-logica-servidor/
        ├── paso1.sql
        ├── paso1.png
        ├── paso2.sql
        ├── paso2.png
        ├── paso3.sql
        └── paso3.png
```

## Subir tus entregas

Haz **commit** en tu **fork** y abre un **Pull Request** hacia el repositorio del curso.
